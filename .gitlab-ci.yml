image: node:fermium-buster-slim

pages:
  stage: build
  script:
    - npm install
    - npm run build
    - mv public _pub && mv dist public
  artifacts:
    paths:
      - public
  only:
    - master
    - dev
